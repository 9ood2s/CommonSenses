# CommonSenses 다음 스토리 제작 핸드오프

최종 갱신: 2026-08-16 (Asia/Seoul)

이 문서는 다음 세션이 기존 작업을 훼손하지 않고 새로운 스토리 콘텐츠를 제작하도록 남기는 별도 인수인계 문서다. 새 세션에서 사용자가 주는 소재·학습 목표·톤·분량이 항상 정본이다. 다만 이번 세션에서 실제로 지적된 학습 게임 로직 누락은 반복하지 않는다. 날짜별 작업 기록은 `dev.log`에 남기고 핸드오프는 이 문서에서만 관리한다.

## 1. 현재 저장소와 보존 대상

- 작업 경로: `/Users/9ood2s/work/CommonSenses`
- 현재 배포 브랜치: `main`
- GitHub Pages는 `main` 브랜치의 저장소 루트(`/`)를 공개한다.
- 공개 목록: `https://9ood2s.github.io/CommonSenses/`
- 새 작업을 시작하기 전에 반드시 `git status --short --branch`를 다시 확인한다.
- 사용자 소유 미추적 디렉터리 `docs/`와 `lesson-slide-generator/`가 있다. 둘 다 임의로 열어 고치거나, 삭제하거나, 새 콘텐츠 커밋·배포에 포함하지 않는다.
- 체크아웃·리셋·정리 등으로 기존 작업을 잃게 만들지 않는다.

## 2. 이번에 완료한 콘텐츠 이름 한 줄

**완료 콘텐츠: 02. 두 번째 광복절 · THE SECOND AUGUST**

- 소스 폴더: `/Users/9ood2s/work/CommonSenses/second-august/`
- 배포 주소: `https://9ood2s.github.io/CommonSenses/second-august/`
- 구성: 5막 20장, 장마다 서로 다른 2문제, 총 40문제
- 캐릭터: 동일한 일본 애니메이션풍의 생성 이미지 12종
- 학습 주제: 한국사, 사료 읽기, 사건의 원인·결과와 시간 순서
- 특정 연예인·그룹·소속사를 재현하지 않는 가상 서사다.

## 3. 사용자가 실제로 지적한 누락과 오류

다음 항목은 이번 제작에서 처음 누락되었거나 잘못 구현되어 사용자가 직접 지적한 내용이다. 다음 콘텐츠에서는 초안 단계부터 체크한다.

1. 원본 학습 게임의 핵심 루프를 빼고 대뜸 문제부터 제시했다.
2. 문제에 나올 핵심 내용을 먼저 읽는 브리핑이 없었다.
3. 문제 제한시간과 줄어드는 시간 게이지가 없었다.
4. 정답 시 오르는 상태값과 오답·시간 초과 벌점이 없었다.
5. 플레이어가 실제로 패배하고 다음 장 해금에 실패하는 구조가 없었다.
6. 상대 인물과 설전한다는 상황·도발·승리 조건 없이 퀴즈만 띄웠다.
7. 노년의 주인공이 젊어졌다는 설정인데 첫 노년 초상과 외롭고 쓸쓸하게 늙어 자연사하는 시간의 흐름이 충분히 제시되지 않았다.
8. 상대 방어력 100에 정답 피해를 40으로 잡아 두 문제를 모두 맞혀도 첫 문제가 세 번째로 반복됐다.
9. 제한시간 숫자와 줄어드는 게이지에 음수 여백을 사용해 두 요소가 겹쳤다.
10. 클리어했거나 정답으로 습득한 지식을 다시 보는 복습 메뉴가 없었다.
11. Supapad CI, Supalabs 회사명, 서비스 도메인 푸터가 빠졌다.
12. 자료 안내의 마지막 고지 문구를 사용자가 지정한 문장으로 맞춰야 했다.

## 4. 다음 콘텐츠에서 누락하면 안 되는 학습 루프

시각 디자인과 이야기 형식은 자유롭게 바꿀 수 있지만, 사용자가 별도로 폐기하라고 하지 않는 한 아래 기능적 흐름을 기본값으로 유지한다.

`상황이 있는 서사 → 상대의 도발과 설전 진입 → 문제 범위 사전 브리핑 → 제한시간 설전 → 결과와 상태 변화 → 승리 해금 또는 패배 재도전 → 습득 지식 복습`

### 서사와 설전 진입

- 질문 전에는 반드시 그 질문이 등장할 만한 사건, 대화, 갈등 상대와 목적이 있어야 한다.
- 상대 이름·역할·도발 문장·승리 조건을 보여 준다.
- 문제를 카드처럼 갑자기 던지지 않는다.

### 사전 브리핑

- 퀴즈에 실제로 쓰이는 개념, 핵심 흐름, 근거와 오답 구별 포인트를 문제 전에 제공한다.
- 브리핑을 읽지 않아도 되는 장식으로 만들지 말고 설전의 정답 근거와 직접 연결한다.
- 원본 `vibe-coding/index.html`의 `brief()`와 설전 흐름을 새 콘텐츠 설계 전에 다시 확인한다.

### 제한시간과 상태값

- 각 문제에는 숫자로 보이는 남은 시간과 줄어드는 게이지가 함께 있어야 한다.
- 시간 숫자와 게이지는 서로 다른 레이아웃 행에 두고 겹치게 만드는 음수 여백을 쓰지 않는다.
- 플레이어 상태와 상대 방어 상태를 동시에 보여 준다.
- 현재 구현 기준: 정답은 상대 논리 `-50`, 평판 `+2`; 오답은 신뢰 `-40`, 평판 `-6`; 시간 초과는 신뢰 `-40`, 평판 `-8`이다.
- 새 콘텐츠에서 수치를 바꿀 수는 있지만 정답 보상, 오답·시간 초과 벌점, 실제 패배 가능성은 반드시 남긴다.

### 문제 수와 반복 방지

- 현재 콘텐츠는 장마다 서로 다른 문제 2개를 한 번씩만 낸다.
- 두 문제를 모두 맞히면 즉시 승리한다.
- 하나라도 놓치면 두 번째 문제까지 처리한 뒤 그 장은 실패하고 브리핑부터 재도전한다.
- 같은 시도 안에서는 문제 배열을 `% length`로 순환시키지 않는다. 앞 문제가 세 번째로 반복되면 안 된다.
- 문제 ID, 질문 본문, 정답 근거의 중복을 데이터 검사로 확인한다.

### 패배와 해금

- 패배하면 다음 장을 해금하지 않는다.
- 오답·시간 초과로 떨어진 평판은 저장한다.
- 실패 결과에서 무엇이 부족했는지 알리고 브리핑부터 재도전할 수 있게 한다.
- 승리·패배·정답 수·오답 수·시간 초과·평판 변화를 결과 화면에서 보여 준다.

### 기억 보관함

- 정답으로 습득한 지식은 즉시 `LEARNING` 상태로 복습 메뉴에 나타난다.
- 장을 클리어하면 그 장의 핵심 근거가 `CLEAR` 상태로 열린다.
- 질문, 핵심 사실, 해설, 장의 근거를 다시 읽을 수 있어야 한다.
- 상단 기억 수치, 장 목록, 결과 화면, 최종 화면에서 복습 메뉴에 접근할 수 있게 한다.

## 5. 이야기·시간 흐름·캐릭터 제작에서 배운 점

- 회귀·성장 설정이면 회귀 전의 상태를 화면으로 충분히 보여 준다. 대사 한 줄로 과거를 요약하지 않는다.
- `두 번째 광복절`에서는 백발과 주름이 있는 80세 강해원을 첫 장면에 보여 주고 `2084 → 2034 → 2035 → 2048 → 2076 → 2084 → 2027`의 날짜와 장소를 화면에 반영했다.
- 추락, 관계 단절, 혼자 사는 중년, 요양원 노년, 방문자 없는 마지막 날, 자연사를 거쳐 회귀해야 인과가 이어진다.
- 다음 스토리는 이 줄거리 자체를 복제하지 않는다. 사용자가 새로 주는 소재가 정본이다.
- 캐릭터는 반실사보다 일관된 일본 애니메이션풍 래스터 이미지를 사용한다.
- 동일 인물의 중립·당황·후회·결연·미소 등 표정 변화를 대화 맥락에 맞춰 준비한다.
- 동일 인물이 장면마다 다른 사람처럼 변하지 않도록 외형 기준을 먼저 고정한다.
- 초상화는 시작 전에 preload/decode하고, 같은 장면에서 불필요하게 DOM을 다시 만들지 않는다.
- 모바일 375px부터 인물이 충분히 크게 보이는지, 탭 번쩍임·강한 밝기 변화·느린 초상화 표시가 없는지 확인한다.

## 6. 브랜드와 고정 표기

- 상단에는 Supapad CI 가로 락업을 둔다.
- CI는 확정 H 칩을 사용한다: 아쿠아 `#0d9bb0` 둥근 칩, 흰색 기하 `ㅅ`, 앰버 `#dc8a0f` 사람 점.
- 워드마크는 소문자 `supapad`, `supa` 800 / `pad` 400 무게다.
- 상단 CI 전체를 `https://supapad.co`에 연결하고 새 탭으로 연다.
- 하단 푸터에는 `supalabs`, `supaclass.co`, `supapad.co`를 함께 표시한다.
- 브랜드 위계는 `supalabs → supaclass · supapad`다. 은퇴한 옛 이름은 새 화면에 사용하지 않는다.
- 현재 자료 안내 마지막 문구는 정확히 다음과 같다.

> 단순한 학습용 서사이며 특정 단체의 주장이나 사상을 담지 않았습니다.

## 7. 현재 구현·검증·배포 메모

- 주요 파일: `second-august/index.html`, `styles.css`, `story.js`, `app.js`
- 저장 키: `commonsenses.second-august.v2`
- 정적 HTML/CSS/JS이므로 별도 빌드 과정은 없다.
- 스크립트·스타일 변경 뒤 브라우저 캐시가 이전 로직을 재사용하지 않도록 쿼리 버전을 갱신한다.
- 마지막 배포에서는 `styles.css?v=20260816-2`, `story.js?v=20260816-2`, `app.js?v=20260816-2`를 사용했다.
- 검증 완료: JS 문법, HTML ID 연결, CSS 중괄호, 로컬 자산 참조, 20장·40문제·40개 고유 ID, 장마다 2문제, 문제 순환 제거, 정답 2회 클리어, 타이머·게이지 분리.
- GitHub Pages 배포 후에는 빌드가 최신 커밋 SHA를 가리키는지 확인하고, 공개 HTML·JS·CSS·주요 이미지의 HTTP 200과 수정 문자열을 직접 확인한다.
- iOS Safari 실기기 검증은 아직 하지 않았다.

## 8. 다음 세션의 새 스토리 제작 순서

1. `HANDOFF.md` 전체와 `dev.log`, 현재 `git status --short --branch`를 먼저 읽는다.
2. `docs/`와 `lesson-slide-generator/`를 포함한 기존 사용자 작업을 보존한다.
3. 새 스토리는 새 폴더에 만들고 `second-august/`를 덮어쓰지 않는다.
4. 사용자가 주는 새 소재·학습 목표·주인공·톤·분량을 정본으로 삼는다.
5. UI를 만들기 전에 장 수, 장별 문제 수, 고유 문제 ID, 상태값, 승패 조건, 해금 조건, 저장 구조, 복습 구조를 먼저 표로 점검한다.
6. 최소 한 장을 `서사 → 설전 진입 → 브리핑 → 제한시간 문제 → 승패 → 기억 보관함`까지 끝까지 연결한 뒤 나머지를 확장한다.
7. 각 장에서 문제를 내기 전에 필요한 내용을 실제로 제공했는지, 같은 시도에서 동일 문제가 반복되지 않는지 자동 검사한다.
8. 캐릭터 외형표와 실제 화면 크롭을 정한 뒤 일관된 애니메이션풍 표정 이미지를 생성한다.
9. 375px 모바일에서 타이머·게이지·초상화·선택지·브랜드가 겹치지 않는지 확인한다.
10. 배포 시 새 폴더와 목록 변경만 의도적으로 커밋하고 사용자 소유 미추적 폴더는 제외한다.

## 9. 다음 세션 작업 이름과 시작 한 줄

**다음 작업 이름: CommonSenses 03 · 새로운 학습 스토리 제작**

새 세션 첫 지시와 함께 아래 한 줄을 전달한다.

> `/Users/9ood2s/work/CommonSenses/HANDOFF.md`와 `/Users/9ood2s/work/CommonSenses/dev.log`를 먼저 전부 읽고 Git 상태를 확인해라. `docs/`와 `lesson-slide-generator/` 및 기존 콘텐츠를 보존한 채 새로운 스토리를 별도 폴더에 만들고, 이번 사용자의 새 프롬프트를 정본으로 삼아라. 시각·서사는 자유롭게 설계하되 `상황 서사 → 상대가 있는 설전 → 문제 범위 사전 브리핑 → 제한시간과 상태·벌점이 있는 고유 문제 → 승패·해금/재도전 → 습득 지식 복습` 로직은 사용자가 명시적으로 바꾸지 않는 한 누락하지 마라. 캐릭터는 일관된 일본 애니메이션풍 표정 이미지를 사용하고 모바일 겹침·번쩍임·늦은 초상화 표시를 처음부터 피하라.
