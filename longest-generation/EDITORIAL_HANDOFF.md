# 《최악의 세대》 대본 편집 인계

최종 갱신: 2026-08-16 (Asia/Seoul)

> 이 문서는 작업 이력이다. 다음 세션의 단일 정본 인계서는 `FINAL_SCRIPT_HANDOFF.md`이며, 현재 소스 지문·사용자 지적 전체·48장 구조·전수 검증 순서는 그 파일을 우선한다.

## 현재 정본

- 작업 폴더: `/Users/9ood2s/work/CommonSenses/longest-generation/`
- 사용자 교정 기록: `USER_EDIT_LOG.md`
- 사용자 수정 원본: `/Users/9ood2s/Downloads/longest-generation-script-edits-202608160621.json`
- 검토 화면: `story-review.html`
- 최종 데이터 구성: 12막, 48장, 대사 249개, 문제 144개, 인생 선택 12개, 결말 5개

2026-08-16 사용자 교정본의 45개 필드는 `story.js`, `expansion-war.js`, `expansion-politics.js`에 반영했다. 교정본에는 맞춤법·띄어쓰기·문장부호만 손봤다. 이 45개 필드는 사용자가 직접 정한 문체와 진행 방식의 표본이므로 다음 세션에서 임의로 원복하거나 전면 재작성하지 않는다.

## 2026-08-16 전 장 편집 완료 범위

- 문체 기준과 수정 순서를 `EDITORIAL_GUIDE.md`에 정리했다.
- `story-review.html`과 실제 플레이가 원본 `beats` 배열을 그대로 따라가게 고쳤다. 문제 사이에 대사가 있으면 `문제 → 다음 장면 → 다음 문제` 순서로 재생되고 검토 화면도 같은 순서로 보인다.
- 4~48장의 사건 원인, 이동과 전달 경로, 만남의 이유, 인물별 손해와 감정, 문제 전 근거 노출을 장별로 다시 확인하고 필요한 장면을 확장했다.
- 후반부의 `메모`는 모두 없앴다. 첫 편집에서 `장부`를 피하려고 만든 `피란단·속환계·군량 출납표·배급표·연표첩·기록소·확인소` 같은 말도 2차로 걷어 냈다. 호적·양안·군적·물목·수결·장계처럼 당대 명칭이 분명한 용어만 남기고, 나머지는 누가 무엇을 세고 적고 옮겼는지 생활 행동으로 풀었다.
- `종이가 사람을 삼킨다`, `장부가 무기가 된다`처럼 뜻을 비유에 맡기던 문장은 누가 무엇을 보고, 옮기고, 확인하고, 잃는지 드러나는 행동과 결과로 바꿨다.
- 노년 서준의 일반 초상 두 장을 단정한 도포 차림으로 교체했다. 원래 누더기 초상은 삭제하지 않았다.
- 45개 사용자 교정 키 가운데 37개는 그대로다. 2장의 첫 달 사건 순서와 겹친 5개, 음식마다 `죽`을 반복하지 말라는 사용자 요청과 겹친 3개는 명시적 수정 예외로 `USER_EDIT_LOG.md`와 `verify-story.js`에 기록했다.

## 2026-08-16 첫 달 사건 순서 재수정

2장 `이름 없는 첫 달`의 정본 순서는 다음과 같다.

`조선에서 깨어남 → 우물가 사람들과 조우 → 연화와 만남 → 류태석과 우연히 마주침 → 마을에서 잡일 → 류씨 마름과 실랑이 → 묶인 채 관아로 끌려감 → 문서와 이웃 증언 대조 → 임시 판정 → 서준이라는 이름을 얻음`

2장의 문제는 별도 묶음이 아니라 사건 사이에 들어간다.

`q42 신분 주장의 근거 → 관아로 끌려간 장면 → q41 호적의 쓰임 → 세 이웃의 증언 → q43 호적의 한계 → 판정`

`연표`는 현대 교실의 별명인 `연표충`에만 남겼다. 조선 시대 장면, 게임 전술 문구, 결과 문구에서는 외운 해·시험 공부·앞일·구체적인 종이와 기록으로 바꿨다.

추가 문체 기준: 인물의 걱정과 목적을 `사람 수와 식량을 센다`, `같은 무게로 적는다`, `숫자로 사람을 지킨다`처럼 계산이나 비유로 요약하지 않는다. 연화가 사람을 걱정하면 부상자를 눕히고 물을 먹이고 끼니를 마련하는 행동을 쓰며, 서준이 기억력을 쓰면 누가 두 그릇을 받고 누가 빠졌는지 찾아내는 결과를 보여 준다.

## 2026-08-16 생활 용어 2차 점검

- `피란단`은 장면에 따라 `세 집 사람들`, `함께 길을 떠난 세 집`, `피란 행렬`로 바꿨다.
- `소·계·표·명세`를 붙여 만든 단체·문서·임시 기관 명칭을 원본 대본에서 제거했다.
- `실무자·책임자·담당자·영수증` 같은 현대 사무어는 구체적인 관직·역할·관인의 수결·생활 행동으로 고쳤다.
- 역사 설명과 문제에서는 실제 제도어를 유지하되, 앞 장면에서 그 말이 가리키는 물건과 손해를 먼저 보여 주도록 했다.
- 사용자 교정 45개 필드는 이 점검 뒤에도 모두 그대로임을 `verify-story.js`로 확인했다.

## 적용한 문체 기준

1. 문장 길이와 호흡, 설명과 대사의 비율
2. 현대식 구어와 조선 시대 인물 말투의 구분
3. 사건 전에 필요한 원인·만남·이동·소문을 보여 주는 방식
4. 인물의 욕망, 두려움, 손해가 대사와 행동으로 드러나는 방식
5. 서준의 내면 독백, 연화와의 관계, 류태석의 반응이 누적되는 방식
6. 역사 지식을 문제에서 갑자기 꺼내지 않고 앞 장면에서 경험하게 하는 방식

같은 단어나 표현을 반복해서 고칠 필요가 있을 때는 사용자가 고친 한 사례를 기준으로 동일 맥락에만 일관되게 적용했다. 단순 문자열 전체 치환은 하지 않았다.

## 검토 화면과 재생성 방법

`story-review.html`은 원본 데이터를 편집하기 위한 스냅샷이다. 현재 2장 화면과 실제 플레이에서 다음 순서를 브라우저로 확인했다.

`도입 장면 → 설전 진입 → q42 → 관아 장면 → q41 → 이웃 증언 → q43 → 판정과 이름 → 다음 장`

대본 원본을 고친 뒤에는 프로젝트 폴더에서 아래 명령을 차례로 실행한다.

```bash
node generate-story-review.js
node verify-story.js
```

첫 명령은 현재 `story.js`와 `expansion-*.js` 실행 결과를 검토 HTML에 다시 넣는다. 둘째 명령은 구성 수, 문제 ID, 인생 선택 위치, 문제 사이 장면의 순차 재생 지원, 이미지 파일, `메모` 잔존, 사용자 교정 키와 허용된 예외를 검사한다.

## 노년 서준 초상

- 일반 노년: `assets/characters/jun-old-respected.png`
- 부드러운 노년: `assets/characters/jun-old-soft-respected.png`
- `story.js`의 `jun-old`, `jun-old-soft`가 위 파일을 사용한다.
- 두 초상은 기존 얼굴·화풍·구도를 유지하고 찢어진 겉옷과 헝겊 조각만 깨끗한 짙은 도포와 단정한 머리띠로 고쳤다.
- 결말별 전용 초상과 향촌 원로 와이드 장면은 기존처럼 사회적 지위와 결말 맥락에 맞는 자산을 쓴다.

## 보존 규칙

- 사용자 수정 45개 키는 `USER_EDIT_LOG.md`의 정본 범위로 취급하되, 사용자 요청과 직접 겹친 8개 명시적 예외는 현재 문장을 새 정본으로 삼는다.
- 새 사용자 JSON을 받으면 안정 키 단위로 비교하고, 변경된 필드만 반영한다.
- 사용자 수정 문장을 다른 장에 적용할 때는 어휘를 복사하는 대신 문장 호흡·감정 원인·사건 연결 원칙을 맞춘다.
- 역사 사실, 연도, 인물 나이와 이동 경로를 바꾸면 48장 전체 연표와 연결 대사를 다시 검사한다.
- 기존 저장 파일, 로더, 이미지, 다른 프로젝트 폴더를 삭제하지 않는다.
- 4~48장 전 장 편집과 생활 용어 2차 점검을 완료했다. 다음 수정은 사용자가 검토 화면에서 지정한 안정 키와 구체적인 피드백을 기준으로 좁혀서 반영한다.

추가 문체 정본: 기록 행위에는 반드시 생활상의 목적을 붙인다. 귀환자 이름은 가족을 찾고 귀향길 식량을 내주기 위해 적으며, 소식 없는 이름은 다음 사신의 기록과 다시 맞추기 위해 남긴다. 과거 경험의 반복은 `어린 시절과 같은 주장`처럼 줄이지 말고, 서준이 무호적 때문에 류씨 집 종이 될 뻔했던 억울함처럼 실제 사건을 다시 밝힌다. 전후 양전은 `소유 문서와 실제 경작의 충돌`로 요약하지 말고 땅문서와 경계 말뚝이 사라진 원인, 옛 소유를 주장하는 사람, 전후에 밭을 일군 사람의 다툼을 순서대로 쓴다.

피란 장면에서는 일반 백성이 종이 명부나 사본을 나누어 읽지 않는다. 어른들이 몇 집씩 맡아 얼굴을 익히고, 집마다 다른 색 천을 묶고, 아이 손을 잡고, 나루와 절 같은 다시 만날 곳을 입으로 되풀이한다. 글은 향리·관아 아전·사행 서기가 배급·호적·군량·포로 몸값처럼 실제 행정에 필요할 때만 쓴다. 후반 43~48장과 다섯 결말도 같은 원칙으로 다시 고쳤다.

음식은 모든 굶주림과 돌봄을 `죽`으로 처리하지 않는다. 첫 만남의 찬 보리밥, 열병 환자의 좁쌀미음, 나루 부상자에게 먹인 볶은 보릿가루, 평양 피란촌의 조·보리밥, 가도 부두의 물에 만 보리밥처럼 장면별로 구체화했다. 연화가 판단할 때마다 밥과 솥에 빗대던 말투도 없앴다. `죽은 문서는 밥을 못 먹어`는 `땅문서 종이 한 장이 사람을 먹여 살려 주지는 않아. 살아 돌아와서 밭을 일굴 사람이 먼저야`로 고쳤고, 실제로 사람을 먹이는 장면에만 음식 표현을 남겼다. 이괄의 난 뒤 류씨 집 처분도 현대식 `벌금과 추징`과 어려운 `적몰`을 삭제하고, 장남의 가담 확인 뒤 류씨 명의로 남아 있던 재산을 나라에서 몰수하는 사건으로 고쳤다. 이미 값을 치르고 주인이 바뀐 논은 류씨 재산이 아니어서 제외한다. 인조실록에 나오는 `관에서 이미 팔아넘긴 역적의 집을 다시 거두지 않는다`는 별개의 처리도 근거 설명에서 구분했다. 14장의 `송사 비용`은 류태석이 관아를 드나들고 증인을 불렀지만 논을 되찾지 못한 행동으로 풀었다. 현재 검토 스냅샷 지문은 `sha256:9cef443b27bdd4b3a1a24d6c7c01cecde6deb2779b6011a525697a54541a4d63`다.

## 새 세션 시작 문장

> `/Users/9ood2s/work/CommonSenses/HANDOFF.md`, `/Users/9ood2s/work/CommonSenses/longest-generation/EDITORIAL_HANDOFF.md`, `/Users/9ood2s/work/CommonSenses/longest-generation/EDITORIAL_GUIDE.md`, `/Users/9ood2s/work/CommonSenses/longest-generation/USER_EDIT_LOG.md`를 먼저 전부 읽고 Git 상태를 확인해라. 사용자 교정 필드를 정본으로 보존하되 사용자 요청과 직접 겹친 8개 예외는 현재 문장을 유지하고, 새 사용자 JSON이나 검토 화면의 안정 키를 기준으로 지적된 장만 좁혀 수정해라. 모든 끼니를 `죽`으로 처리하거나 조선의 역적 처분을 `벌금·추징·적몰`, 송사에 든 수고를 `송사 비용`으로 쓰지 마라. 원본 대본을 고친 뒤에는 `node generate-story-review.js`와 `node verify-story.js`를 실행해 검토 스냅샷과 전체 구성을 다시 검증해라.
