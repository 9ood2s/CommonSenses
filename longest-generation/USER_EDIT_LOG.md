# 사용자 대본 수정 반영 기록

## 2026-08-16 · 1차 사용자 교정본

- 원본 파일: `longest-generation-script-edits-202608160621.json`
- 원본 위치: `/Users/9ood2s/Downloads/longest-generation-script-edits-202608160621.json`
- 내보낸 시각: `2026-08-16T06:21:47.604Z`
- 검토 HTML의 소스 리비전: `adb35d4`
- 검토 HTML의 소스 지문: `sha256:7e51ae49ed117410085bfe21997909b85aa70205dd466e6f17147b9a835457ac`
- 사용자 JSON 파일 SHA-256: `474344731fd6ac430f6a5a6a24804a2b190334115088b3e2ed2addb2d9fdac49`
- 반영 항목: 총 45개

이번 반영분은 사용자가 직접 고친 대본을 정본으로 삼았다. 보조 작업은 맞춤법, 띄어쓰기, 문장부호, 대화 따옴표와 명백한 문법 오류만 바로잡았다. 사건의 뜻, 인물의 태도, 장면 순서, 사용자가 고른 단어와 구어체는 임의로 확장하거나 다른 문체로 바꾸지 않았다.

### 반영 범위

- 1장: 장면·요약·근거·설전 문구 8개, `q01`·`q02`·`q125` 문구 8개 — 합계 16개
- 2장: 장면·요약·근거·설전 문구 14개, `q42`·`q43` 문구 5개 — 합계 19개
- 3장: 제목·요약·근거·설전·장면 문구 10개 — 합계 10개
- 소스 파일별: `story.js` 24개, `expansion-war.js` 19개, `expansion-politics.js` 2개

### 교정 원칙과 특기 사항

- `떠있다 → 떠 있다`, `안봤냐 → 안 봤냐`, `함부러 → 함부로`, `흙바닭 → 흙바닥`, `네번 → 네 번`처럼 표기만 고쳤다.
- 원문의 말줄임표와 직접 발화는 화면에서 깨지지 않도록 `…`, `“…”`, `<em>…</em>` 표기로 통일했다.
- 사용자가 남긴 “이 글자만 작게”라는 편집 메모는 대사에 노출하지 않고 `<small>이</small>`로 구현했다.
- 동일 단어를 나머지 대본에 일괄 치환하거나 문체를 전파하는 작업은 하지 않았다.
- 다른 장의 사건 순서, 감정선, 대사 보강도 이번 반영 범위에서 제외했다.

다음 사용자 교정본을 받을 때는 이 기록과 새 JSON의 안정 키를 비교해 새 변경만 반영한다. 사용자가 고친 45개 필드는 다음 세션의 문체 참고 정본이며, 명시적인 요청 없이 이전 문장으로 되돌리지 않는다.

## 2026-08-16 · 전 장 편집 뒤 보존 확인

- 4~48장 문체·사건 연결 편집 뒤 `verify-story.js`로 45개 안정 키를 직전 정본과 비교했다.
- 결과: `45 unchanged`.
- 사용자 JSON의 SHA-256도 위에 적은 `474344…ac49`와 일치했다.
- 이번 편집은 1~3장의 사용자 정본 문장을 다른 말투로 덮어쓰지 않았다.
- 이후 검토 HTML의 원본 스냅샷은 `editorial-20260816`, `sha256:d95ce66748a1aa20aebbb33719de367581521eae473e9cca5993736a835aa3db`이다.

## 2026-08-16 · 생활 용어 2차 점검 뒤 보존 확인

- 사용자가 `피란단`을 비롯한 임의의 한자어·조어·현대 사무어를 생활인이 쓸 법한 말로 고치라고 요청했다.
- 4~48장과 선택·결말에서 해당 표현을 문맥별로 고쳤으며 단순 문자열 전체 치환은 하지 않았다.
- `verify-story.js`로 다시 비교한 결과 사용자 교정 45개 안정 키는 `45 unchanged`였다.
- 새 검토 HTML의 원본 스냅샷은 `editorial-20260816`, `sha256:6f1f982629dbb670d40d8899e4623d93101e50d8f36202d6dea6b958be716b81`이다.

## 2026-08-16 · 2장 사건 순서 재지정에 따른 명시적 예외

사용자가 2장 첫 달을 `마을 사람들과 조우 → 연화와 만남 → 류태석과 우연히 만남 → 잡일 → 무호적 문제로 마름과 실랑이 → 관아로 끌려감` 순서로 다시 지정했다. 이 요청을 반영하면서 기존 사용자 교정 키 가운데 아래 5개는 문장과 위치가 직접 겹쳐 다시 썼다.

- `story.ch02.line.006.text`
- `story.ch02.line.007.text`
- `story.ch02.line.008.text`
- `story.ch02.line.009.text`
- `story.ch02.line.010.text`

나머지 사용자 교정 40개 키는 이전 정본과 같다. `verify-story.js` 결과는 `40 unchanged; 5 user-authorized changes`다. 검토 화면과 실행기는 문제 사이의 대사를 건너뛰지 않고 원본 배열 순서로 재생한다. 감정과 목적을 숫자 세기나 비유로 대신한 후반 문장까지 고친 뒤의 원본 스냅샷은 `editorial-20260816`, `sha256:4dc5654ea65ddba4b2858d96f9ae895f37266dfc1fcb506e5a3a03aacfba9bae`다.

## 2026-08-16 · 사용자 문체 지적 추가 반영

사용자는 남원성의 민간인 피해, 피로인 귀환 기록의 목적, 류씨의 군량 운송 떠넘기기, 서준의 무호적 경험 반복, 전후 양전 분쟁을 추상적인 결론이 아니라 원인과 생활상의 결과로 쓰도록 문장을 직접 교정했다. 이어 일반 백성의 높은 문맹률과 피란 상황을 고려하면 종이 명부·사본을 주고받는 설정이 맞지 않는다고 지적했다. 피란 장면은 얼굴 확인, 색 천, 담당 어른, 구두로 되풀이하는 만남 장소로 바꾸고, 글은 향리·아전·사행 서기의 실제 행정 업무로 제한했다. 해당 지적은 4장 이후 문장에만 반영했으며 기존 사용자 교정 45개 키의 보존 범위에는 새로 겹치지 않는다. 최신 검토 스냅샷은 `editorial-20260816`, `sha256:ea6595cbaaedc197e1b5637cddfc1959ab7b5b9febec412948defb67b7082ec6`다.

## 2026-08-16 · 음식 반복 수정에 따른 명시적 예외

사용자가 굶주림·피란·간호 장면마다 `죽`이 반복된다고 지적해 음식이 실제로 필요한 형편을 장면별로 다시 썼다. 이 과정에서 기존 교정 키 가운데 아래 3개가 직접 겹쳤다.

- `story.ch02.line.003.text`: 첫 만남의 보리죽을 찬 보리밥 한 덩이로 수정
- `story.ch02.line.011.text`: `내일도 죽 나오는 줄 알지 마`를 `내일도 밥 나오는 줄 알지 마`로 수정
- `story.ch03.line.003.text`: `죽 한 그릇 값`을 `보리 한 되 값`으로 수정

2장 사건 순서 예외 5개를 합쳐 사용자 승인 예외는 8개다. `verify-story.js` 결과는 `37 unchanged; 8 user-authorized changes`다. 나머지 음식 장면은 찬 보리밥, 좁쌀미음, 조와 보리를 섞은 밥, 볶은 보릿가루, 물에 만 보리밥 등 장소와 형편에 맞게 구체화했다.

같은 요청에서 15장의 `벌금과 추징`은 이괄군 가담이 확인된 뒤 류씨 명의로 남은 재산을 나라에서 몰수하는 사건으로 고쳤다. 이미 값을 치르고 주인이 바뀐 논은 류씨 재산이 아니어서 제외했다. 인조실록에 나오는 `관에서 이미 팔아넘긴 역적의 집을 다시 거두지 않는다`는 처리는 이와 구분해 근거 설명과 자료 안내에 반영했다. 이어 사용자가 `적몰`과 `송사 비용`도 어렵거나 현대적으로 뭉뚱그린 표현이라고 지적했다. 대본에서는 `몰수`로 통일하고, 송사는 류태석이 며칠씩 관아를 오가며 증인을 불렀지만 논을 되찾지 못한 행동으로 풀었다. 연화가 판단할 때마다 `밥·그릇·솥`에 빗대던 대사도 걷어 내고, 땅문서만으로 사람을 먹여 살릴 수 없으며 살아 돌아와 밭을 일굴 사람이 먼저라고 직접 말하게 했다. 최신 검토 스냅샷은 `editorial-20260816`, `sha256:9cef443b27bdd4b3a1a24d6c7c01cecde6deb2779b6011a525697a54541a4d63`다.
